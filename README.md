# docker-trusty-mysql-server
Dockerfile of mysql-server on ubuntu:trusty.

It needs the workaround in this Dockerfile to run mysql-server on ubuntu:trusty. (https://github.com/docker/for-linux/issues/72)
